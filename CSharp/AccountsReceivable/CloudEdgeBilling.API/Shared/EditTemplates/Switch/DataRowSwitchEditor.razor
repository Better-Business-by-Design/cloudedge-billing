@typeparam TGrid where TGrid : IDataRow
@inherits DataRowEditor<TGrid, bool>

<MudSwitch T="bool" Checked="@Value" Color="Color.Primary" Size="Size.Small" CheckedChanged="@(async (bool b) => await HandleValueChanged(b))">
    @(Value ? "True" : "False")
</MudSwitch>
